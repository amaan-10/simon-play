let gameSeq = [];
letuserSeq = [];

let started = false;
let level = 0;

document.addEventListener("keypress", function () {
  if (statred == false) {
    console.log("game is started");
    started = true;
  }
});
